# Public production URL for auth emails, password reset, and group invite links.
# Required for Capacitor builds (native shell uses localhost at runtime).
VITE_APP_URL=https://jion-andal.github.io/rtodo
